# RE:TR4CKS 要件定義書（Android版）

- バージョン: v2.15
- 更新日: 2026-09-11
- ステータス: 技術構成の確定まで完了。MVP実装に着手可能

> Remember. Replay. Rediscover. Revisit.

---

## 1. 概要

### 1.1 アプリ概要

ローカル音楽ファイルを対象に、曲の指定区間のみを再生しながら次々と曲を切り替えていく音楽プレイヤーアプリ。

### 1.2 コンセプト

> 忘れていた曲に、次々と出会い直す体験

- 短時間で多くの楽曲を再生する
- 懐かしい曲・埋もれた曲を再発見する
- 作業用BGM・流し聴き用途にも使える

### 1.3 コア価値

- 時短で多くの曲を聴ける
- 懐かしい曲との再会
- 流れを止めない再生体験

---

## 2. ターゲットユーザー

- ローカルに大量の音楽を保存しているユーザー
- 昔の楽曲をよく聴いていたユーザー
- フル再生ではなく部分的に楽しみたいユーザー

---

## 3. 対応プラットフォーム・制約

- Android のみ
- ローカルファイルのみ対応（ストリーミング非対応）
- ネットワーク接続不要

### 3.1 メディアアクセス権限（決定：2026-09-02）

- Android 13 以降は `READ_MEDIA_AUDIO`、それ以前は `READ_EXTERNAL_STORAGE` を使用する
- 初回起動時に権限をリクエストする
- 拒否された場合は説明画面を表示し、設定アプリへの導線を用意する

---

## 4. コア機能

### 4.1 再生機能

- ローカル音楽ファイルの再生
- 再生 / 停止 / 次の曲 / 前の曲
- シャッフル再生
- リピート再生（OFF → 全曲リピート → 1曲リピート の3状態。普通のプレイヤー準拠）

### 4.2 区間再生機能（メイン機能）

**パラメータ**

- 再生開始位置（秒）
- 再生時間（秒）

**指定方式**（決定：2026-09-02、％対応は2026-09-11に不要と判断し撤回）

- 秒指定（絶対値）のみに対応する
- 曲の長さに対する相対指定（％）は当初「秒とセットで両対応」の方針だったが、使う場面が薄いと判断しスコープから外した
- 秒指定が曲の長さを超える場合の扱いは「境界処理」（下記）で解決済み

**設定スコープ**（決定：2026-09-02）

- 区間設定はキュー全体で共通の**全体設定**を使用する
- プレイヤー画面のスライダー操作は、常に**全体設定の変更**として扱う（曲ごとの個別設定を暗黙に作らない）

**曲ごとの個別設定（却下：2026-09-11）**

- 曲ごとに区間設定を上書きする機能は実装しないと決定した。全体設定のみを使う

**動作**

- 指定した区間のみを再生し、終了後に次の曲へ遷移する

**境界処理**（決定：2026-09-02）

- 開始位置＋再生時間が曲の長さを超える場合：超過分は再生せず、**曲の終端でフェードアウトして次の曲へ**遷移する
- 開始位置が曲の長さを超える場合：**0秒（曲の先頭）から**再生する

### 4.3 RUSHモード（自動スキップ）

本アプリの中心となる再生モード。

UI上の表示名は「メドレー」（メドレー ON / OFF、**2026-09-11 決定**）。「RUSH」は
初見で機能が伝わらないという判断による変更。UI表示名は `Skip` → `RUSH` →
「メドレー」の順で2度変わっており、古いスクリーンショットやメモに `Skip` の
表記が残っていてもこの変遷を辿れる。コード内の変数名・関数名や、本書での呼称は
引き続き RUSH のまま（検索性のため書き換えない）。

**RUSH ON（デフォルト）**

- 区間再生を有効にし、区間終了時にフェードアウトして次の曲へ自動遷移する

**RUSH OFF**

- 区間再生を無効にし、通常の音楽プレイヤーとして曲を**最初からフル再生**する
- 開始位置へのシークも行わない
- 曲が最後まで再生されたら次の曲へ遷移する

**再生中に ON/OFF を切り替えた場合**（決定：2026-09-02）

- 切り替えは**次の曲から適用**する（シャッフルボタンと同じ感覚）
- 区間設定（Start / Length）の変更も同様に次の曲から反映する
- 再生中の曲を区間の開始位置へシークし直すことはしない
- ただし **OFF にした場合のみ**、再生中の曲の区間カットも即座に解除し、そのまま最後まで再生する（シークはしない）

### 4.4 フェード（必須）

**フェードアウト**

- 区間の終了直前に音量を徐々に下げる
- フェード時間：1〜5秒（デフォルト2秒）

**フェードイン**（決定：2026-09-03）

- 区間の開始直後に音量を徐々に上げる（デフォルト1秒）
- ネイティブ側で音量を制御しているため実装コストが小さく、追加した

**クランプ**

- 実効フェード時間 = `min(設定値, Length ÷ 2)`
- イン・アウトそれぞれに適用するため、両者が重なることはない
- 例：Length=3秒 / Fade=5秒 → 実効フェード 1.5秒

**クロスフェードは実装しない**（決定：2026-09-03）

- 曲を重ねる真のクロスフェードは対象外とする。曲は重ならない（疑似クロスフェード）
- Media3 に組み込みAPIが無い（androidx/media#2 が2021年から未解決）。実装するには
  ExoPlayer 2台構成と、それを1つの論理プレイヤーとして MediaSession に見せる層が必要
- 実測で曲間の無音が平均約50msに収まっており、現在の方式で十分滑らかと判断した

---

## 5. キュー管理

### 5.1 キュー生成モード（排他）

- 全曲
- アーティスト（複数選択可）
- アルバム（複数選択可）

生成の導線はライブラリ画面の選択モードに統合する（→ 10.3）。

### 5.2 キュー生成ルール

- 選択条件に一致する楽曲をすべて追加する
- 同一楽曲の重複は排除する

### 5.3 再生終了時の挙動・リピート（2026-09-08 実装）

リピートは3状態で、押すたびに **OFF → 全曲 → 1曲 → OFF** の順に巡る
（一般的なプレイヤー準拠）。プレイヤー画面とウィジェットの両方に置く。

| 状態 | キューの最後まで再生したとき |
|---|---|
| OFF | 停止する |
| 全曲 | 先頭へ戻る。1巡の完了として扱い、次の巡を作る（6.2） |
| 1曲 | 同じ曲を繰り返す。1巡の進捗は動かない |

設定はネイティブ側のプレイヤーが持ち、QueueStore に控える。アプリを終了しても、
ウィジェットから起こしたときも前回の設定で始まる。既定は全曲。

JS 側はキューを積むときにリピートを上書きしない。上書きすると、利用者が選んだ
設定が再生のたびに戻ってしまう。

---

## 6. シャッフル再生

「シャッフルにしても同じ曲ばかり流れ、大半の曲が一生再生されない」問題の解消を目的とする。
RUSHモードとは**独立した機能**であり、RUSH ON / OFF のどちらでも同じように動作する。

### 6.1 基本仕様（普通のプレイヤー準拠）

- キュー内の曲から**順列を1つ生成**して順に再生する（再生ごとに独立抽選する方式は採らない）
- 1巡内で同一曲の重複なし
- シャッフルON/OFFの切り替えは**次の曲から適用**する（再生中の曲はそのまま継続）
- シャッフルON中の「前の曲」は**再生履歴を遡る**（元の並び順の前の曲ではない）
- シャッフルOFF時の並び順は、再生元となった一覧の表示順に従う（例：楽曲タブなら曲名順）

### 6.2 1巡状態の永続化（本アプリの肝）

UI上は「1巡」の概念自体を出さず、プレイヤー画面ヘッダーには**キュー内の位置**
（`status.index + 1` / `queue.length`）だけを表示する（**2026-09-11 決定**）。
シャッフルキューでは `RetracksPlayer.setQueue(ordered, state.cursor, key)` で
ネイティブのキューに順列全体が入り `state.cursor` から再生が始まるため、
`status.index + 1 === progress.played`・`queue.length === progress.total` が
常に成り立つ。「1巡」という語も「シャッフル」という語も不要で、`playFrom`
（曲を順番に再生）由来のキューでも同じ式でそのまま位置が出せる（それまでは
シャッフルキューのときだけ数字が出たり消えたりしていた）。「1巡」は本書・
コード内（`reconcile()`・`isCycleComplete()`等）の呼称としては引き続き使う。
デバッグ画面（14章・状態カード）は「1巡」表示のまま据え置いている。永続化
されたシャッフル状態のカーソルという別概念を見せている場所であり、UI上の
「1巡」の唯一の出口として残す判断（RUSH→メドレーと同じ型）。

- 生成した順列と現在位置（カーソル）を、**アプリ再起動をまたいで保存**する
- 起動時は保存済みの順列とカーソルを復元し、続きから再生する
- 1巡し切るまで、既に再生した曲は再び選ばれない
- これにより「毎回シャッフルし直して順列の先頭付近だけを聴く」状態を防ぐ

**保存単位**

- キューの生成条件ごとに順列を保持する（全曲 / アーティスト選択 / アルバム選択 など）
- 別のキューを再生してから戻っても、元のキューの順列と位置は失われない
  （※これにより旧「シャッフル状態の保持」仕様は自動的に満たされる）

**ライブラリが変化したとき**

- 削除された曲：順列から取り除く
- 追加された曲：順列の**未消化部分にランダムに挿入**する（新しく入れた曲がすぐ出てくるように）

**1巡し終わったとき**

- リピートON：順列を作り直して2巡目に入る
  - カーソルを0に戻す実装とコストが変わらないため、多様性が出る「作り直し」を採用する
  - 2巡目の先頭が1巡目の末尾と同じ曲になった場合は引き直す（同じ曲が2回続くのを防ぐ）
- リピートOFF：停止する

### 6.3 進捗の可視化

- プレイヤー画面ヘッダーにキュー内の位置（例：`109/982`）を表示する
  （**2026-09-11 決定**：UI上は「1巡」の概念を出さない → 6.2）。
  シャッフルキューでは結果的に1巡の進捗と同じ数字になるため、
  「ライブラリをちゃんと一周できている」ことは変わらず可視化される
- デバッグ画面では従来どおり1巡の進捗として表示する（6.2）

---

## 7. 再生制御仕様

### 7.1 再生開始

- 曲開始時、指定した開始位置へシークする

### 7.2 スキップ判定

- 再生位置ベースで区間終了を判定する

### 7.3 フェード処理

- 区間終了直前でフェードアウトを開始する
- フェード終了と同時に次の曲の再生を開始する

### 7.4 読めない曲の扱い（2026-09-07 決定）

他のアプリで曲が削除されても、キューにはその曲が残る。走査は都度おこなわないため、
キューを作った時点のファイルが今もあるとは限らない。

- 読み込みに失敗した曲は**黙って次へ送る**。停止しない
- 送ったあと prepare をやり直し、元の再生状態（再生中／一時停止）に戻す
- リピートが有効だと次の項目は必ず存在するため、キューを一周しても
  1曲も鳴らせなければそこで停止する

キューから消しはしない。順番と1巡の進捗は JS 側が持っており、ネイティブ側で
項目を抜くと番号がずれるため。消えた曲が一覧から消えるのは次の走査時。

### 7.4.1 区間の付け外しはメディアソースを作り直す（2026-09-08 実測）

区間は MediaItem の ClippingConfiguration に焼き込むため、RUSH を切り替えたら
次の曲以降を組み直す必要がある。このとき **replaceMediaItems を使ってはならない**。

Media3 は曲が同じならメディアソースを作り直さず、MediaItem の参照だけ差し替える。
区間なしのソース（ProgressiveMediaSource）は差分判定で ClippingConfiguration を
見ないため、区間を**付ける**変更が黙って捨てられる。外す方は区間ありのソースが
比較するので効く。この非対称のせいで「RUSH の ON だけ効かない」という形で出た。

`removeMediaItems` + `addMediaItems` に置き換えてソースごと作り直させる。

組み直す範囲は**いま鳴っている曲以外のすべて**。後ろだけにすると、切り替えた
あとに前へ戻ったとき、そこは切り替え前の状態が残る（RUSH を切ったのに数曲は
短縮のまま鳴る）。前側を消すと現在位置の添字がずれるので、**後ろ側を先に**
処理する。いま鳴っている曲だけは組み直さない（頭から鳴り直すため）。

紛らわしいのは、`getMediaItemAt(i).clippingConfiguration` は**新しい値を返す**点。
設定は入っているのに再生は変わらないので、値を読むだけの確認では見抜けない。
実際に鳴らして曲の長さを見ること。

### 7.5 再生中のセッションの引き継ぎ（2026-09-07 決定）

アプリのプロセスだけが落ちて再生が続いている場合や、ウィジェットから再生を
始めた場合、JS はあとから起動して鳴っているセッションに合流する。

- **キューはネイティブ側から貰う**。JS の保存を当てにして番号だけを
  当てはめてはならない
- 1巡の進捗は、保存してある順列が鳴っているキューと**完全に同じ並び**の
  ときだけ引き継ぐ。長さの一致だけでは判定しない
- どのキューを鳴らしているかの識別子もネイティブ側に控える

これを守らないと、全曲シャッフルの順列に別キューの番号を当てはめてしまい、
画面と音が食い違う（2026-09-07 に実際に発生）。

---

## 8. バックグラウンド機能

- バックグラウンド再生に対応する
- 画面ロック中も再生を継続する
- スリープ中も再生を維持する

### メモリと再生の安定性（2026-09-06 実測）

外出時に再生が突然止まる症状を調査した結果を記録する。

**原因はメモリではなくクラッシュだった。**

  ForegroundServiceDidNotStartInTimeException
    at RetracksWidgetProvider.onReceive

ウィジェットから `startForegroundService` でサービスを起こしたあと、期限内に
`startForeground` を呼んでいなかった。修正済み（→ PlaybackService.onStartCommand）。

**再生中のプロセスは保護されている**

  oom_score_adj = 200 / Proc #1 / fg-service
  画面OFFで4分経過しても再生継続

上限は 1001（空きプロセス）。前面のUI（0）に次ぐ位置で、メモリ不足で終了させられる
可能性は低い。終了履歴に残っていた LOW_MEMORY はいずれも `state=empty` のプロセスに
対するもので、再生が終わったあとの通常の回収だった。

**Pulsar との比較**

同じ端末の同じ時点で `dumpsys meminfo -d` を取った。

| | Pulsar | RE:TR4CKS |
|---|---|---|
| TOTAL PSS | 325.9MB | 228.7MB |
| Graphics | 211.0MB | 0.06MB |
| Native Heap | 73.9MB | 1.9MB |
| Bitmap | 490枚 / 43.0MB | 286枚 / 27.6MB |
| **SWAP PSS** | **2.0MB** | **131.7MB** |

**この数字から「RE:TR4CKS の方が軽い」とは言えない。** 測定時の状態が揃っておらず、
RE:TR4CKS 側は背景に回ってメモリの多くが退避された状態だった（SWAP PSS 131.7MB）。
一方 Pulsar は退避がほとんど無い。

言えるのは「**RE:TR4CKS が Pulsar より明らかに重いという結果は出ていない**」まで。

参考として、RE:TR4CKS の別の時点の値：
- 前面で一覧を大量にスクロールした直後：PSS 326MB
- 再生中・画面OFF・4分経過：PSS 362MB（Native 141MB / Graphics 84MB / Bitmap 482枚 59MB）

測定条件によって 229〜362MB の幅がある。常時 362MB を占有し続けるアプリではない。

なおビットマップは1枚あたり約122KB（およそ174×174相当）で、デコード時の縮小は
正しく効いている。残るのは枚数であり、表示に必要なぶんに近い。

**効果があった対処 / なかった対処**

- 効果あり：一覧の描画範囲を絞る（getItemLayout、windowSize）。PSS 543→326MB、
  Graphics 118→25MB
- 効果あり：背景での画像キャッシュ破棄。ビットマップ 790→633枚
- 効果なし：背景で一覧の画像描画をやめる。撤去済み
- 効果なし：expo-image 導入そのもの（単体では 277→543MB と悪化。描画範囲の
  調整と併せて初めて下がった）

**方針**：メモリ削減それ自体を目的にした最適化はしない。行うのは、LMK のリスクや
再生の安定性に対する根拠を示せる場合に限る。再現性のあるメモリ起因の不具合が
出るまでは追加の最適化を行わない。

**この記録を書くにあたっての注意**：当初、Pulsar の測定値と RE:TR4CKS の別時点の
測定値を並べて「同等」と結論づけていた。比較として成立していなかったため訂正した。
測定値を比較する際は、取得した時点とアプリの状態が揃っているかを必ず確認すること。

### 既知の制約：通知をスワイプで消した場合（2026-09-03 実測）

通知をスワイプで消すと、再生は続くが通知は戻らない。曲送りや一時停止など
**再生状態が変わったタイミングで自然に復活する**。

アプリ側から復活させる方法を検証したが、いずれも効果が無かった。

- 通知はスワイプ後もシステム上は posted のまま残る（`activeNotifications` に残存）
- そのため同じ内容を再投稿しても再表示されない
- `cancel` してからの再投稿、`stopForeground` を挟んでの再投稿も効果なし

ユーザーが明示的に消した通知をアプリ側から復活させられないのは、Android 14 以降の
方針に沿った挙動と考えられる。無理に押し通すとフォアグラウンドサービスの状態を
崩し、バックグラウンド再生（本章の必須要件）を損なうリスクの方が大きいため、
プラットフォームの挙動として受け入れる。

### 既知の不具合：ウィジェットから起こしたときに通知が出ない（2026-09-09 実機で発覚・修正済み）

**症状**：アプリを一度も開いていない状態でウィジェットから再生すると、再生自体は
問題なく始まるが、通知にもロック画面にも何も出ない。ロック画面には（再生していない）
Pulsar のメディアカードが代わりに表示される。アプリを一度開いてから再生すると
正しく出る。

**原因は2つ重なっていた。**

1. **`POST_NOTIFICATIONS` 権限を一度もリクエストしていなかった**
   Android 13（API 33）以降、この権限が無いと `NotificationManager` への投稿は
   フォアグラウンドサービス用も含めてすべて黙って握りつぶされる（サービス自体は
   問題なく前面で動き続けるため気づきにくい）。マニフェストにも無く、実行時
   リクエストのコードもどこにも無かった。`dumpsys package` の runtime permissions
   一覧に `POST_NOTIFICATIONS` 自体が存在しないことで発覚した。

   対処：マニフェストに `<uses-permission>` を追加し、`RetracksPlayerModule` に
   `requestNotificationPermissionAsync()` を生やして起動時にリクエストする
   （`src/playback.tsx` の起動シーケンスに追加。結果は問わずライブラリ読み込みは
   続行する）。

2. **`MediaSession.Builder.build()` はセッションを Media3 の通知管理に登録しない**
   権限を直しても直らなかった。`dumpsys notification` で実際に投稿されている
   通知を調べると、`channel=retracks_playback`（自前の仮通知）のまま
   `actions` も `category` も付かない状態で固定されており、Media3 本来の
   通知（`channel=default_channel_id`, `category=transport`, `vis=PUBLIC`）に
   一度も差し替わっていなかった。

   `MediaSession$Builder.build()` を `javap -c` で逆アセンブルして確認したところ、
   中身は `new MediaSession(...)` を返すだけで、サービス側の通知管理
   （`MediaNotificationManager`）には一切触れていない。普段「勝手に登録される」
   ように見えていたのは、JS 側の `MediaController.Builder(...).buildAsync()` が
   接続する際に `onGetSession()` 経由で暗黙に登録されていたため。ウィジェットから
   `startForegroundService` で起こした経路は、サービス内で `player.play()` を
   直接呼ぶだけでコントローラが一切つながらないため、この暗黙の登録が起きない。
   セッションは存在し `active=true` で `MediaButtonReceiver` にも正しく載るのに、
   通知管理の対象としては認識されないという食い違いだった。

   対処：`PlaybackService.onCreate()` でセッションを作った直後に
   `addSession(mediaSession)` を明示的に呼ぶ。あわせて
   `setShowNotificationForIdlePlayer(SHOW_NOTIFICATION_FOR_IDLE_PLAYER_ALWAYS)`
   （既定だと曲が止まっている・未再生の間は通知を出し渋る）と、
   `onStartCommand()` で `player.play()` の直後に `triggerNotificationUpdate()`
   を呼び、仮通知から本物の通知への差し替えを明示的に急がせている。

**調査の要点（再発時のために）**：`dumpsys media_session` でセッションが
`active=true` で最上位にあることを確認しても、それは「セッションが存在し正しく
機能している」ことの証明にしかならず、「通知がちゃんと出る」こととは別問題。
実際に投稿されている通知の中身（`dumpsys notification` の `channel=` /
`actions=` / `category=` / `vis=`）を、正常に動いている他アプリ（今回は Pulsar）
と見比べて初めて「仮通知のまま固定されている」ことが分かった。

---

## 9. 外部操作

### 9.1 通知操作

- 再生 / 停止
- 次の曲

### 9.2 イヤホン操作

- 再生 / 停止
- 次の曲

---

## 10. UI設計

### 10.1 画面構成

普通の音楽プレイヤーとしての使用感は **Pulsar** をベースとする。

- ライブラリ画面（タブ切り替え：アーティスト / アルバム / 楽曲 / プレイリスト）
  - キュー作成はこの画面の**選択モード**に統合する（専用画面は設けない）
- プレイヤー画面（再生中画面）
- 設定画面
- ミニプレイヤー（常時表示）

### 10.2 プレイヤー画面（メイン）

**概要**：再生・設定をすべて集約した中心画面

```
┌──────────────────────┐
│  ← Queue / 設定       │
├──────────────────────┤
│                      │
│      🎵 曲名          │
│   Artist / Album     │
│                      │
│    ─────────────     │
│    00:20 / 00:55     │
│    ─────────────     │
│                      │
│   [⏮] [▶/⏸] [⏭]      │
│                      │
│  Shuffle: ON         │
│  Skip: ON            │
│                      │
├──────────────────────┤
│ ▶ 区間設定           │
│ Start :  [====●----] 20s
│ Length:  [====●----] 70s
│ Fade  :  [==●------] 2s
│                      │
├──────────────────────┤
│ ▶ Queue Info         │
│ Mode : Artist (3)    │
│ Tracks: 128曲        │
└──────────────────────┘
```

**要素**

| グループ | 内容 |
|---|---|
| 再生情報 | 曲名 / アーティスト名・アルバム名 / 再生区間（開始・終了） |
| 再生操作 | 前の曲 / 再生・停止 / 次の曲 |
| 状態表示 | シャッフル ON/OFF / スキップ ON/OFF |
| 区間設定（スライダー、折りたたみ可能。既定は閉じた状態 → 2026-09-11） | 開始位置（秒） / 再生時間（秒） / フェードイン・フェードアウト時間（秒） |
| キュー情報 | 現在のモード（全曲 / アーティスト / アルバム） / 曲数 / キュー内の位置（例：109/982 → 2026-09-11、6.3） |

**再生中画面としての一般的な要素**（Pulsar 準拠）

- 大きなアートワーク表示
- シークバー（現在位置 / 全体長）とシーク操作
- シャッフルボタン、リピートボタン（OFF / 全曲 / 1曲）
- キュー内容の表示
- 曲情報の表示（アルバム名、フォーマット情報など）

### 10.3 キュー作成（ライブラリ画面の選択モード）

**概要**：専用画面は設けず、ライブラリ画面上の**長押し → 複数選択**でキューを作る。
アーティストやアルバムをまとめて選んで「簡易プレイリスト」として流す使い方を主眼に置く。

**導線**

1. アーティスト / アルバム / 楽曲タブで項目を**長押し**すると選択モードに入る
2. 以降はタップで複数選択できる（選択数をヘッダーに表示）
3. アクションを選ぶ
   - **再生**：選択対象の楽曲でキューを生成し、プレイヤー画面へ遷移して再生を開始する
   - **プレイリストとして保存**：ユーザー作成プレイリストに保存する（Phase 2）

```
┌──────────────────────┐
│ ✕  3件選択     ▶  ⋮ │  ← 選択数 / 再生 / その他
├──────────────────────┤
│ ☑ Artist A     22曲  │
│ ☑ Artist B     16曲  │
│ ☐ Artist C      4曲  │
│ ☑ Artist D      8曲  │
└──────────────────────┘
```

**仕様**

- 選択条件に一致する楽曲でキューを生成する（同一楽曲の重複は排除 → 5.2）
- 複数選択は同一タブ内で行う
- 「全曲」キューは選択モードを使わず、楽曲タブのシャッフルFABから生成する
- キュー生成後はプレイヤー画面へ自動遷移し、再生を開始する（決定：2026-09-02）

### 10.4 ライブラリ画面

**概要**：端末内の楽曲をタブで切り替えて閲覧・再生する。Pulsar の構成をベースとする。

```
┌──────────────────────┐
│ ≡  RE:TR4CKS   🔍  ⋮ │
├──────────────────────┤
│ アーティスト│楽曲│PL  │  ← タブ
├──────────────────────┤
│                      │
│  （タブごとの一覧）   │
│                      │
│                 (⤮)  │  ← シャッフル再生FAB
├──────────────────────┤
│ 🎵 曲名        [⏸][⏭]│  ← ミニプレイヤー
└──────────────────────┘
```

**タブ構成**

| タブ | 内容 |
|---|---|
| アーティスト | アートワーク付きグリッド。アーティスト名 / アルバム数 / 曲数を表示 |
| アルバム | アートワーク付きグリッド。アルバム名 / アーティスト名 / 曲数を表示 |
| 楽曲 | 全曲のリスト。曲名 / アーティスト名 / 再生時間を表示 |
| プレイリスト | 自動プレイリスト（最も再生された＝再生回数 / 最近の追加曲）＋ ユーザー作成プレイリスト（お気に入りは端末をまたいで持ち運べないため対象外に決定。「最近再生された」は要否保留 → 15章16番） |

**仕様**

- タブは設定で**並び替え・表示非表示**を変更できる（→ 10.7）
  - タブ候補：アルバム / アーティスト / フォルダ / ジャンル / 楽曲 / プレイリスト
- 項目を**長押し**すると選択モードに入り、複数選択からキューを作成できる（→ 10.3）
- 一覧から曲をタップすると、**その一覧をキューとして**再生を開始する（普通のプレイヤー準拠）
  - RUSH ON なら区間再生、RUSH OFF ならフル再生（→ 4.3）
- シャッフル再生用のFABを配置する
- 検索機能を持つ

**並べ替え（曲・アルバムタブ、アーティスト詳細の曲一覧）**（2026-09-11 決定）

曲タブ・アルバムタブ・アーティスト詳細の曲一覧は、ヘッダーの並べ替えアイコンから
軸と方向（昇順/降順）を選べる（メニュー方式。いずれも方向を持つため巡回式の
ボタンにはできない。モーダル自体は `src/components/SortMenu.tsx` として共通化）。

| 画面 | 軸 | 既定 |
|---|---|---|
| 曲タブ | タイトル / アルバム / アーティスト / 長さ | タイトル昇順 |
| アルバムタブ | タイトル / アーティスト / 発売年 | タイトル昇順 |
| アーティスト詳細の曲一覧 | タイトル / アルバム / 長さ / 発売年 | **アルバム昇順** |

- プロバイダの `tracks`/`albums`（タイトル順の正本）は変更しない。各画面が
  表示用に並べ替えた配列を別に持つ（→ `src/sortOrder.ts`）。SettingsProvider に
  持たせると、その値の変更で `PlaybackProvider` の `tracks` が再計算され、
  `artists`/`albums` の導出やライブラリ全体の絞り込みまで連鎖して走り直すため、
  意図的に分離した
- 第2・第3キーは `compareByTrackOrder()`（ディスク・トラック番号順）を流用する。
  「アルバム順」「アーティスト順」「発売年順」で同じアルバム内の曲が散らばらない
  ようにする（アルバム順→アルバム名→ディスク→トラック番号、アーティスト順・
  発売年順→アルバム名→ディスク→トラック番号）。方向（昇順/降順）は主軸にだけ
  効かせ、第2・第3キーは常に自然な順のまま（降順にしたときアルバム内の曲順まで
  逆転すると使い物にならないため）
- アルバムの発売年（`Album.year`。アーティスト詳細では曲が属するアルバムの
  発売年）が無いものは、方向に関わらず常に末尾に寄せる
- 選んだ軸・方向は端末に保存し、次回も同じ状態で開く（→ `StorageKeys.sortOrder`）。
  アーティスト詳細は曲タブとは別のキー（`artistTracks`）で持つ（既定が
  アルバム順と異なるため）
- 「順番に再生」は画面に表示している並び順をそのまま使う（複数選択時も同様）
- アーティスト詳細の曲一覧は、並べ替え機能の追加にあわせて既定をタイトル順から
  **アルバム順**に変えた（2026-09-11）。アーティストページは本来ディスコグラフィで、
  1枚のアルバムの曲がタイトル順でバラバラに並ぶより、アルバム→ディスク→トラック
  番号の方が読みやすいと判断した
- 設定画面の「先頭の The / A・An を無視」は並べ替えの**規則**（同じ軸の中での
  比較方法）であり、ここでいう並べ替えの**軸**とは別物。混同を避けるため設定画面の
  見出しを「並べ替えの規則」に変更した（→ 10.6）

**対象外（v1）・フェーズ2として記録**

- ライブラリ画面のアーティストタブ：軸が「名前」の1つしか無く、並べ替えメニューを
  置く意味が無いため対象外。「アルバムアーティスト」軸を足せば意味が出るが、
  expo-music-library が ALBUM_ARTIST を扱っておらず自前のネイティブクエリが要る
  （`getTrackFolders()` と同じ方式）ため、フェーズ2で改めて検討する
- アルバム詳細の曲一覧：ディスク/トラック番号順が前提のため対象外
- アーティスト詳細の**アルバム一覧**（上部のグリッド）：発売年降順で固定。
  ディスコグラフィは新しい順が見やすく、並べ替えの対象は曲一覧のみとした
- 「追加日」軸：Track に持っていない（`Asset.creationTime` は走査時に取得できるが
  保存していない）。足すならキャッシュ版を5→6に上げる作業が伴うため見送った
  （曲タブ・アーティスト詳細とも）

### 10.5 ミニプレイヤー

**概要**：全画面共通で表示される簡易プレイヤー

```
┌──────────────────────┐
│ 🎵 曲名        [⏸] [⏭] │
└──────────────────────┘
```

**仕様**

- 再生 / 停止
- 次の曲へ
- タップでプレイヤー画面へ遷移

### 10.6 設定画面

Pulsar を参考にした構成。MVPで必須のものと、あれば嬉しいものを分けて考える。

**優先度：高**

- タブ項目の変更（表示するタブの選択と並び替え）（**実装済み・2026-09-10**）
  - 曲・アーティスト・アルバムの3タブを対象に、表示/非表示と並び順を設定できる
  - 最低1タブは表示必須（保存データの破損時は `normalizeTabs()` が全既定へ
    戻す。設定画面側も最後の1枚はスイッチを disabled にして操作自体を防ぐ）
  - 副作用として、全曲シャッフルの導線（FAB）を曲タブの中からライブラリ
    画面レベルへ移した。元々は曲タブの中にしかなく、アーティスト/アルバム
    タブでは出ていなかった（タブを隠せるようになる以上、消えると困る導線を
    タブの内側に置いたままにはできないため）
  - 検索結果のセクション見出しは、タブの表示設定から独立させた
    （`search.sectionSongs/Artists/Albums`）。タブを隠すことは「そのデータを
    扱わない」ことを意味しないため（検索や曲からアーティスト/アルバムの
    詳細画面へは、タブを隠していても到達できる）
- 音楽以外のファイルを除外する（着信音・通知音・アラーム・オーディオブックなど）
  （**実装済み・2026-09-10**）
  - **発見（2026-09-10）**：`expo-music-library` は MediaStore の `IS_MUSIC` を
    一切見ていない（ネイティブ実装の `ASSET_PROJECTION` にも `createSelection()`
    にも登場しない）。着信音・通知音・アラーム・オーディオブック・ポッドキャストが
    曲一覧に素通しで混ざっていた。恐らくこれが「短い曲を除外」を既定ONにした
    主因（通知音は数百msで短いため）。自前ネイティブモジュールに
    `getTrackFolders()` を追加し、`IS_MUSIC` が明示的に0の曲を除外できるように
    した（NULLは音楽として扱う＝除外しない安全側）。デフォルトON
  - ラベルは「着信音・通知音を除外」ではなく「音楽以外のファイルを除外」にした。
    `IS_MUSIC` はオーディオブック・ポッドキャストにも0が立つため、狭いラベルだと
    原因に辿り着けない利用者が出るため
  - 対象・スコープは「短い曲を除外」と同じ制約（下記）
  - 実機計測では「音楽以外を除外」単体で968曲、「短い曲を除外」単体で971曲、
    両方ONでも968曲（実測1027曲中）。通知音等はほぼ短い曲側とも重複しており、
    「短い曲を除外」の既定ONの主因はこちらで解消される。この計測結果を受けて
    「短い曲を除外」の既定値を見直した（下記）
- 除外するフォルダ（自炊音源などIS_MUSICで拾いきれない残り向け）
  （**実装済み・2026-09-11**）
  - 設計変更（2026-09-10）：フォルダ一覧は `expo-music-library` の
    `getFoldersAsync()` を使わず、`getTrackFolders()`（上記）が返す
    フォルダID/名前とスキャン済み `rawTracks` から組み立てる方針に変更。
    `getFoldersAsync()` 由来だと、Android 10未満のフォルダIDが
    `expo-music-library` 内部のハッシュ実装と一致している前提が必要になる
    （自前で完全再現するのはリスクが高い）ほか、件数がアプリ内の絞り込み後の
    実態と食い違う
  - 除外IDの保存は、タブの `normalizeTabs()` とは逆に「正本と突き合わせて
    未知のIDを捨てる」をしない。SDカード未マウント等で一時的に一覧に出ない
    フォルダのIDを捨てると、除外設定が黙って消えるため
  - UI（2026-09-11）：Pulsarを参考にした専用画面（`app/excluded-folders.tsx`）。
    ＋ボタンでモーダルを開き、チェックボックスで複数選択してOK、一覧の各行は
    タップで個別解除。ただし候補フォルダの取得方法はPulsarと異なる。
    Pulsarはファイルシステムを直接ブラウズしており、それには
    `MANAGE_EXTERNAL_STORAGE`（すべてのファイルへのアクセス）が要るが、
    音楽プレイヤーアプリではGoogle Playの審査で基本的に却下される権限
    （海外展開の前提と相性が悪い）。代わりに MediaStore が認識済みの
    フォルダ（`usePlayback().folders`、曲が入っているフォルダのみ）を候補にした。
    曲の無いフォルダを除外する意味は無いため実用上の支障は無い
- 短い曲を除外する（例：5秒未満）（**実装済み・2026-09-10**）
  - **デフォルトはOFF、しきい値5秒**
    - 2026-09-03：既定OFFで決定
    - 2026-09-10：既定ONへ変更（通知音などの混入が主因と推定）。同日中に
      「音楽以外のファイルを除外」を追加
    - 2026-09-11：「音楽以外のファイルを除外」により既定ONにしていた主因が
      解消されたと判断し、既定OFFへ戻した。
      `IS_MUSIC` は MediaStore 全体の規約で、Ringtones/Notifications/Alarms
      配下には端末を問わず0が立つため、この解消は端末依存ではない。
      一方、既定ONのまま残す副作用は一方向（5秒未満の実在する曲—SE・
      インタールード・隠しトラックなど—を黙って隠す）だった
  - しきい値は5〜120秒の範囲で5秒刻み
  - 対象は曲一覧・検索・全曲再生/シャッフル・曲一覧からの複数選択再生に加えて、
    アーティスト/アルバムのトラック数表示・詳細画面の一覧・そこからの複数選択
    再生にも及ぶ（2026-09-11、下記のアーティスト/アルバム導出の作り替えにより、
    どちらも同じ絞り込み済み `tracks` から作られるようになったため）
- アーティスト/アルバムのID（2026-09-11、上記の導出の作り替えに伴う既知の制約）
  - `deriveArtists()`/`deriveAlbums()`（→ src/library.ts）のグループ化キーは
    `artistId || artist` / `albumId || album`。ID が欠けた曲だけ名前に落ちる
    （Android ではほぼ常に ID が入るためレアケース）が、この名前は
    ルートパラメータ（`/artist/[id]` 等。`/` を含む名前で問題になりうる）と
    `buildQueueKey()` の `ids.sort().join(',')`（`,` を含む名前で問題になりうる）
    の両方に流れ込む。同一アーティストの曲の一部だけ `artistId` が欠けていると
    一覧上は2つのアーティストに割れる、という副作用もある。以前は必ず数値の
    MediaStore ID だったため起きなかった種類の問題
- 一覧の並べ替え順（**実装済み・2026-09-10**）
  - 曲・アーティスト・アルバムのタイトル/名前でクライアント側ソートに統一
  - 先頭の "The " を無視するかを設定できる（既定ON）
  - "A"/"An" も別トグルで無視できる（既定OFF）。Pulsar はまとめて1つの設定
    だが、"The" とは流儀の割れ方が違うと判断し、独立したON/OFFにした
    （決定：2026-09-10）
  - この設定画面の見出しは元々「並べ替え」だったが、曲・アルバムタブに
    並べ替えの**軸**（タイトル/アーティスト/アルバム/長さ/発売年）を選べる
    機能が別途入り（→ 10.4）、両方を「並べ替え」と呼ぶと混同するため
    「並べ替えの規則」に変更した（**2026-09-11**）。ここにある設定は
    どの軸で並べ替えるときも効く**比較の規則**であって、軸の選択そのもの
    ではない
- 言語の手動切り替え（**実装済み・2026-09-10**）（要件外・追加項目）
  - 「端末の設定に従う」/ 日本語 / English。選択肢の並びは `SUPPORTED_LANGUAGES`
    （→ 8章）から動的に生成している。言語を増やす際は `app/settings.tsx` の
    `LANGUAGE_LABEL_KEY` にラベルキーを1件足す必要があるが、
    `satisfies Record<LanguagePreference, string>` を付けているため
    追加し忘れは型エラーで必ず検出できる
  - **既知の制約**：ウィジェット（Kotlin/XMLの文字列リソース）は端末言語を見ており、
    この設定には連動しない（→ 8章）
  - **既知の制約（2026-09-11 判明）**：実機の Hermes には `Intl.PluralRules`
    が無い。i18next はこれが無いと英語の単数/複数規則（count===1 なら
    one、それ以外は other）だけのダミーに落ちる。ja/en の2言語は
    `_one`/`_other` を両方明示することで実害を消した（→ src/i18n/locales/ja.ts）
    が、これは対症療法。category が3つ以上ある言語（ロシア語・ポーランド語・
    アラビア語など）を今後足す場合、正しい複数形選択には `Intl.PluralRules`
    のポリフィル導入が要る（i18next の `compatibilityJSON` 設定では解決しない、
    ランタイム自体に機能が無いため）

**優先度：中〜低**

- RUSHのデフォルト値（Start / Length / Fade）（**対応済み・2026-09-11**）
  - 「初期値に戻すボタン」のような専用UIは不要と決定。`src/rush.ts` の
    `DEFAULT_SEGMENT` を直接編集する運用でよいと判断した
    （再生時間を35秒→70秒に変更）
- アクセントカラーの変更（2026-09-11 調査）
  - 現状のアクセント色は `src/theme.ts` の `colors.accent`（#e8912a）/
    `colors.accentDim` の2値のみ。背景・文字色などは対象外で、下記の
    将来検討にあるライト/ダークモード切り替えとはスコープが別（そちらより
    ずっと軽い）
  - 使用箇所は11ファイル42箇所。うち約19箇所はSwitch/Sliderの色など
    JSX内で直接使っており、値が動的になれば自動で反映される。残り
    約23箇所は `StyleSheet.create()`（モジュール読み込み時に1回だけ
    評価される）内にあり、設定値を都度読みにいく形へ書き換えが要る
    （index/player/search/settings/excluded-folders/debug/
    album・artist詳細・Row・MiniPlayer の計10ファイル）
  - ネイティブ側にも1箇所ハードコードがある（通知の「RUSH ON」アイコン
    `retracks_ic_replay_on.xml`）。連動させるならKotlin側で実行時に
    色を塗り直す処理が要る。ウィジェット側には見当たらなかった
  - 変更の反映にアプリ再起動を要求してよいと決定した（2026-09-11）。
    Context経由でのリアルタイム再描画は不要になり、起動時に設定値を
    読んで `colors.accent`/`accentDim` を決める形で足りる

**将来検討（2026-09-10 調査）**

- ライト/ダークモードの手動・自動切り替え
  - 現状 `src/theme.ts` は起動時に1回だけ評価される固定パレットで、全画面が
    直接importしている。動的切り替えには ThemeProvider 化＋全画面のスタイル
    見直しが要る、最も規模の大きい項目
  - `app.json` の `userInterfaceStyle` は `expo-system-ui` 未導入のため現状無効
  - （2026-09-11）アクセントカラー1色だけを変更可能にする話は、これとは
    スコープが別の軽い項目として切り出した（→ 優先度：中〜低）
- ギャップレス再生（2026-09-11、優先度：中〜低から降格）
  - 現状の曲間の無音（実測平均約50ms、→ 4.4）に不満が無いため後回しにした。
    曲間が連続するよう作られたアルバムやライブ音源を聴く場面で欲しくなったら
    再検討する
- 課金モードの案内（広告を消す想定）
  - 広告SDK・IAPライブラリともに未導入。「広告を消す」ためには先に広告自体の
    実装が要るため、設定画面の1項目というより別プロジェクトの規模

このアプリは日本語ユーザー向けに閉じず、海外にも展開する前提で作る
（決定：2026-09-10）。言語・並べ替えなど各機能の既定値はこの前提で選ぶこと。

### 10.7 UI設計方針

- **シンプル設計**：画面数を最小限にする、迷わない操作を優先
- **プレイヤー中心**：設定はプレイヤー画面に集約し、画面遷移を減らす
- **直感操作**：スライダーによる秒数調整、ワンタップ操作
- **情報最小化**：必要な情報のみ表示し、ノイズを排除
- **アイコン**（2026-09-11 決定）：`@expo/vector-icons`（Ionicons）を使う。
  以前は⌕⚙⊞▦☰←✕＋▾⤮✓のようなUnicode文字を`<Text>`で描画していたが、
  フォント依存で線が細く小さく見えるため全画面で置き換えた。線の太さは
  基本アウトライン系（`-outline`）に統一。ただし主要な操作系の意匠は
  文字グリフのまま残している場面もある（ミニプレイヤーの再生/次へは、
  塗りつぶしIoniconsの角丸が「野暮ったい」と判断され、元の文字グリフの
  まま大きさだけ調整した → `src/components/MiniPlayer.tsx`）

---

## 11. UX特徴

- 流し聴きしながら操作可能
- 設定変更が即反映される
- 懐かしい曲をテンポよく再生

---

## 12. 非機能要件

- 再生の安定性
- バックグラウンド動作保証
- 低遅延
- 操作レスポンスの即時反映

---

## 13. 技術方針

調査日：2026-09-02

### 13.1 基本構成

| 領域 | 採用 | 備考 |
|---|---|---|
| フレームワーク | Expo (SDK 57) / React Native 0.86 | New Architecture のみ（RN 0.82 で旧アーキ廃止、0.85 で互換層も削除） |
| ビルド | **ローカルビルド**（Android Studio） | Kotlin を書くため反復コンパイルが前提。Expo Go は不可 |
| **再生層** | **自前の Expo モジュール（Kotlin + Media3）** | 既製ライブラリでは要件を満たせないため（→ 13.2） |
| ライブラリ走査 | expo-music-library | 曲/アルバム/アーティスト/ジャンル/アートワークを取得 |
| キュー・シャッフル管理 | 自前実装（JS側） | 独自の順列＋永続化（→ 6章） |

### 13.2 再生層を自前で持つ理由

**要件 9.1 / 9.2（通知・イヤホンからの「次の曲」）が既製ライブラリで満たせない。**

Android でこれを実現するには、MediaSession が `COMMAND_SEEK_TO_NEXT_MEDIA_ITEM` を
公開し、かつ処理する必要がある。イヤホンの2タップも `KEYCODE_MEDIA_NEXT` として
この経路を通るため、通知ボタンの有無と同じ問題になる。

| 候補 | 判定 | 根拠 |
|---|---|---|
| expo-audio 57.0.4 | ✗ | `AudioMediaSessionCallback.onConnect` が `COMMAND_SEEK_TO_NEXT_MEDIA_ITEM` 等を明示的に `remove()` している。`AudioPlaylist` は `next()/previous()` を持つが、`AudioControlsService` が `AudioPlayer` 型のみ受け付けるため MediaSession に載らない |
| react-native-track-player V4 (4.1.2) | ✗ | レガシーアーキテクチャ用モジュール。RN 0.85 でブリッジ互換レイヤーが削除済みのため現行 RN では動作しない |
| react-native-track-player V5 (@rntp/player) | △ | 要件は満たすが商用ライセンス €999/年〜 |
| react-native-video | ✗ | 単一メディア前提でキュー概念が無く「次の曲」が成立しない |
| react-native-audio-pro | ✗ | アーカイブ済み |

**自前実装を選ぶ判断根拠**

1. 差別化要素（区間再生・フェード・RUSH・1巡シャッフル）がすべて再生層にある。既製プレイヤーの「曲を頭から最後まで流す」前提と根本的に噛み合わない
2. **Android専用**のため iOS 実装が不要で、コストが概ね半分で済む
3. MediaSessionService を自分で持てば、通知/イヤホン/Android Auto が素直に手に入り、フェードもネイティブ側で滑らかに実装できる

### 13.3 自前モジュールの責務

ローカル Expo モジュール（`modules/` 配下）として実装する。

**モジュール側（Kotlin / Media3）**

- ExoPlayer によるローカルファイル再生
- MediaSessionService（通知・ロック画面・イヤホン・Android Auto）
- **区間の切り出し**：`MediaItem.ClippingConfiguration` で開始・終了を宣言し、ExoPlayer
  自身に切らせる。位置をポーリングして終了を検出する方式は精度が出なかったため採らない（→ 13.4）
- **フェードイン／アウト**：音量ランプ。数百ms のずれは聴感に出ないため、こちらは
  50ms 間隔のループで制御する

**JS側**

- ライブラリ走査（expo-music-library）
- キュー生成・シャッフル順列・1巡状態の永続化（→ 6章）
- UI 全般
- 区間設定の解決（`src/rush.ts`。秒/％の吸収もここ）

### 13.4 技術検証の結果（2026-09-03 / Pixel 9a / Android 17)

| # | 項目 | 結果 |
|---|---|---|
| 1 | **通知・イヤホンからの「次の曲」** | ✅ **合格**。ロック画面に前/次ボタンが表示され、イヤホンのタップで次・前・再生・停止すべて動作 |
| 2 | 区間再生の精度 | ✅ ClippingConfiguration へ変更し **±25ms**（変更前は 173〜265ms） |
| 3 | フェードの品質 | ✅ 合格。フェードイン／アウトとも滑らか（→ 下記の注意点あり） |
| 4 | 曲の切り替わりの間 | ✅ **32〜66ms（平均約50ms）** |
| 5 | バックグラウンド動作 | ✅ 合格。画面ロック中も再生継続 |
| 6 | ライブラリ走査の速度 | ✅ 合格。**1025曲 / 69アーティスト / 195アルバムを 3983ms** |

**#1 が通ったことで 13.2 の判断（再生層を自前で持つ）が裏付けられた。** 構成は確定とする。

**走査についての知見**

- ライブラリが 1025曲 で 1000件を超えるため、`getAssetsAsync` のページング（上限1000）が必須だった
- 起動のたびに約4秒かかるため、曲一覧はキャッシュする設計が要る。シャッフル順列の
  永続化（→ 6.2）と保存先を揃えて検討する

**再測定（2026-09-10）**：`getTrackFolders()`（IS_MUSIC・フォルダ情報の取得。
ページングと並行に投げる設計）を追加した後、デバッグ画面の「再走査」で
**1027曲 / 916ms**。2026-09-03時点の3983msとは条件が違う（3983msは起動時の
コールドスキャン＝キャッシュ無し・OSのページキャッシュも冷えた状態。916msは
アプリ稼働中の手動再走査＝直前にも MediaStore へ問い合わせている状態）ため、
両者を直接比較してベースラインが縮んだとは言えない。この再測定で確認したいのは
「`getTrackFolders()` 追加による劣化が無いこと」のみで、それは満たしている。

**区間の切り出し方式について**

当初は 50ms 間隔で再生位置を監視して終了を検出していたが、実測のズレが 173〜265ms と
監視間隔の 4〜5倍に達した。原因はメインスレッドが UI 描画で混み、Handler のコールバックが
遅延すること。切り出しを `MediaItem.ClippingConfiguration` に委ねる方式へ変更した。
ExoPlayer がサンプル単位で切って自動的に次へ進むため、ポーリングが判定経路から外れる。
開始位置へのシークも不要になる。

代償として、区間設定を変更した際に MediaItem を組み直す必要がある。ただし再生中の曲まで
差し替えるとその曲が区間の先頭から鳴り直してしまうため、`replaceMediaItems` で
**現在の曲は触らず次の曲以降だけを差し替える**（決定：2026-09-03）。
設定変更は「次の曲から反映」となる。

**フェードの実装で判明したこと（重要）**

`player.volume` は「いま鳴っている音」ではなく、**これから AudioTrack へ書き込むサンプル**に効く。
ExoPlayer は音声を先読みして書き込むため、再生位置を見てそのまま音量を絞ると、実際に
ゲインがかかるのは概ね 250ms 先の音になる。結果として次の症状が出た。

- 前の曲の末尾がフェードされずフル音量のまま鳴る
- 次の曲の冒頭に、前の曲用のフェードアウトのゲインがかかる

**対処**：ゲインの計算に `再生位置 + WRITE_AHEAD_MS(250ms)` を使い、音量の適用先と
位置を合わせた。実機で症状が解消することを確認済み。

**残るリスク**：250ms は実測に基づく経験値で、端末や Android バージョンによって
先読み量が変わる可能性がある。ずれが再発する場合は、PCM サンプルに直接ゲインを掛ける
`AudioProcessor` 方式が根本解になる（音声パイプラインの中でゲインがサンプルと一緒に
流れるため、位置APIの粗さともバッファの先読みとも無関係になる）。ただし ExoPlayer は
ギャップレス再生時に音声シンクをフラッシュしないため、曲の境界をサンプル列の中で
知る仕組み（区間長のFIFOと、シーク時の再同期）を別途作る必要がある。

**位置APIの粗さについて**

`player.currentPosition` は実測で約260ms ごとにしか更新されない。そのまま使うと
フェードが階段状になるため、値が変わらない間は経過時間で補間している。

**実装上ハマった点（再発防止のため記録）**

- `MediaController` はメインスレッドからしか操作できない。Expo Modules の `Function` /
  `AsyncFunction` は JS スレッドで動くため、コントローラに触る処理は明示的に
  メインスレッドへ回す必要がある
- prebuild が生成する構成は `expo-splash-screen` の存在を前提としている
- **再生中にネイティブヒープが増え続け、OSに強制終了される**（2026-09-11、実機で発覚・修正）
  - 症状：再生中、Javaヒープは健全なまま（25MB/256MB）ネイティブヒープだけが
    数分でGB単位まで増え続け、Android（Pixel）の `MemoryLimiter:AnonSwap` に
    強制終了された
  - 原因：`usePlayback()` の Context `value` を毎レンダー作り直していたため、
    再生位置を250ms間隔でポーリングする `setStatus()` のたびに `value` が
    新しい参照になり、React の仕組み上それを使う全画面が再生中ずっと毎秒
    複数回再レンダーされ続けていた。ネイティブヒープが増えるのは、この頻度
    で Fabric のシャドウツリー側の確保が積み上がっていたため
  - 切り分け方（この3点で「レンダー駆動」と確定できた。次に似た症状が
    出たときに使える手順）
    1. バックグラウンドにすると増加が完全に止まる（画面を描画していない
       間は起きない）
    2. 一時停止中（値が1つも変わっていない）でも止まらない
       （＝表示内容ではなく再レンダーの回数そのものが原因）
    3. ポーリング間隔を伸ばすと増加率がほぼ比例して下がる
  - 対処（→ `src/playback.tsx` / `app/player.tsx` / `src/components/MiniPlayer.tsx`）
    - 250msごとに変わる `status` を専用の別 Context（`usePlaybackStatus()`）
      に分離し、`usePlayback()` の残り（tracks/artists/albums/queue/操作関数
      等）は `useMemo` で参照を安定させた。生の再生位置が要る3箇所
      （MiniPlayer・プレイヤー画面・デバッグ画面）だけが毎秒再レンダーされる
    - プレイヤー画面のシークバーとキュー一覧の行は、値が同じでも再レンダー
      されるたびにネイティブ側の処理が走っていたため `React.memo`/
      `useCallback` で安定化した。インラインの `onPress={() => skipTo(index)}`
      のような「行ごと・レンダーごとに新しい関数」を渡すと memo が無効化
      されるので注意（`QueueRow` は `onPress(index)` を受け取る形にして
      親から1つの安定した関数を渡すよう直した）
    - `RetracksPlayer.getStatus()` は中身が同じでも毎回新しいオブジェクトを
      返すため、フィールドを浅く比較して変化が無ければ `setStatus` に前の
      値をそのまま返させ、React に再レンダーを止めさせるようにした
      （`statusEquals()`）
    - ポーリング間隔自体も250ms→1秒に変更（プログレスバーとしては体感上
      問題ない）
  - 実機での効果：ライブラリ画面（実際に落ちた経路）は修正前GB級/数分の
    増加 → 完全にフラットに。プレイヤー画面（生の再生位置UIがあり一番
    厳しい）は毎分75〜97MB → 毎分12〜13MBまで低減。実測2点（1秒間隔で
    12.5MB/分・5秒間隔で4MB/分）を線形回帰すると、レンダー駆動の増加が
    約0.18MB/回、レンダーと無関係な残りが約2MB/分と見積もれる
    （測定点が2つしか無いため目安）
  - **残件**：プレイヤー画面はまだ完全にはフラットになっていない。上記の
    見積もりが正しければ、これ以上分割しても毎分2〜4MB程度が下限。ただし
    増加は「アプリが前面かつ画面が点いている間」だけで、バックグラウンド
    再生（画面消灯中含む）では起きない。警告閾値（3GB）に達するには画面を
    点けたままプレイヤー画面を3時間以上表示し続ける必要があり、通常の
    使い方では起きにくい。プレイヤー画面のさらなる分割（再生位置に依存する
    部分を独立した小さなコンポーネントへ切り出し、画面本体の再レンダー
    頻度自体を下げる）は、区間設定・キュー・シーク・リピートが絡む画面
    構造に手を入れることになるため、実際に長時間セッションで問題になった
    ときに着手する
  - 副次的な改善候補（未着手）：再生中でもアプリがバックグラウンドの
    ときはポーリング自体を止める（`AppState` を見る）。メモリへの効果は
    無い（バックグラウンドでは元々増えない）が、毎秒ネイティブ呼び出しを
    続けるのは電池の無駄なので、別の改善として価値がある
- **`getStatus()` は最大200ms古いキャッシュを返す──JS側の対処だけでは直せない**
  （2026-09-11、曲送り操作の反応が鈍く見えた不具合の調査で発覚）
  - 症状：次の曲ボタン等を押してから、曲名・ジャケット・ヘッダーのキュー位置が
    画面に反映されるまで最大1秒かかる。メモリリーク対策でポーリング間隔を
    250ms→1秒にして以降に目立つようになった
  - 紛らわしかった点：`onTrackChange` を受けて `setStatus(RetracksPlayer.getStatus())`
    を呼ぶ対処を JS 側だけに入れても、体感が変わらなかった。`getStatus()` が
    最大200ms古いスナップショットを返すため、直前の `status` と
    `statusEquals()` で同値と判定され、実質 no-op になっていた
  - 真因：`RetracksPlayerModule.kt` の `getStatus()` は保持している `snapshot`
    をそのまま返すだけで、呼ばれた時点でプレイヤーを読みに行かない。その
    `snapshot` の更新は200ms間隔のポーリング（`snapshotRunnable`）だけが
    担っており、状態が変わった瞬間（曲の切り替わり・リピート設定変更）に
    同期更新する経路が無かった
  - 対処：スナップショット計算を `refreshSnapshot()` として切り出し、
    `onMediaItemTransition`（曲が変わった瞬間）と `setRepeatMode()` の直後で
    同期的に呼ぶようにした（ネイティブの再ビルドが必要）。あわせて
    `getStatus()` の宣言（`RetracksPlayerModule.ts`）に「即時性は保証しない」
    旨のコメントを追加した
  - 同根の別バグ：`cycleRepeat()` も `RetracksPlayer.getStatus().repeatMode` を
    読んでから次の値を計算していたため、200ms以内にリピートボタンを連打すると
    2回目が古い `repeatMode` から計算され、同じ値に戻ってしまっていた
    （オフ→全曲→オフ→全曲…と巡回が進まない）。同じ `refreshSnapshot()` の
    追加で解消・実機確認済み
  - やらなかったこと：真因が分かる前に「ジャケット画像自体の読み込みが遅い
    のでは」という仮説でアルバムアートの先読み（`Image.prefetch`）を投機的に
    追加したが、真因（上記）を直したら体感が解消したため、効果未検証のまま
    リバートした（本アプリの他の性能改善は実測を伴うが、これだけは無かった
    ため）。「音はすぐ切り替わるのにジャケットだけ遅れる」を実際に観測した
    ときに、改めて実測とセットで検討する
  - 教訓：このモジュールで「JS側に対処を入れたのに体感が変わらない」ときは、
    まず `getStatus()` がネイティブ側のポーリング間隔ぶん古い値を返している
    可能性を疑う

### 13.5 リリース準備（安定性向上）

14章 Phase 2 の4項目（→そちら）の進捗をここに記録する。

**1. リリースビルド＋自前キーストア**（2026-09-11・対応済み）

- `android/`ディレクトリ自体がリポジトリ管理外（Expo prebuildで生成される
  前提、ルートの`.gitignore`参照）だと判明。つまり`android/app/build.gradle`
  への変更は、この`android/`が再生成（`expo prebuild --clean`等）されると
  失われる。今のところこのプロジェクトでは`android/`は一度生成されたら
  手で触り続ける運用（ウィジェットのdrawable等、本章の随所を参照）なので、
  同じ扱いとして直接編集した。config plugin化はしていない（次に`android/`を
  作り直す必要が生じたときは、この節を読んでから作業すること）
- `keytool`で`android/app/release.jks`を生成。DN（証明書の識別情報）は
  実運用上ほぼ意味を持たないため`CN=RE:TR4CKS`等の一般的な値にした
- パスワードは`openssl rand`で生成し、`android/keystore.properties`
  （`.gitignore`で明示的に除外。`android/`丸ごとの除外とは別に、鍵の
  流出リスクが大きいものなので二重に対策した）に平文で保持。
  `build.gradle`はこのファイルの有無を見て、無ければ debug 鍵に
  フォールバックし警告を出す（CI等、鍵を持たない環境でも壊れないように）
- **`release.jks`と`keystore.properties`の中身（2つのパスワード）は、
  この端末が壊れる／初期化されると復旧できない。パスワードマネージャ等の
  外部の安全な場所に必ずバックアップすること。** 失くすと、同じ署名では
  二度とアプリを更新できなくなる（Google Play・配布方法を問わない）
- `./gradlew assembleRelease`が成功することを確認（初回56分48秒。Reanimated
  等のネイティブコードを4 ABI分、最適化ありでコンパイルするため長い。
  以降は変更差分のみになるはずで、毎回この時間はかからない）
- `apksigner verify --print-certs`で自前鍵（`CN=RE:TR4CKS`）の署名を確認
- 既存のdebug鍵インストールをアンインストールしてから`app-release.apk`を
  実機にインストールし、起動・権限ダイアログ・ライブラリ走査（992曲）・
  区間再生（メドレー）・キュー表示まで一通り動作することを確認

**2. JSのエラーバウンダリ**（2026-09-11・対応済み）

- `src/components/ErrorBoundary.tsx`を新設し、`app/_layout.tsx`の最も外側
  （`GestureHandlerRootView`より外）に配置。`SettingsProvider`/
  `PlaybackProvider`自体が投げた場合も拾うため
- 捕まえた例外は`src/crashLog.ts`経由でAsyncStorageに直近10件保存し、
  デバッグ画面（状態カードの並び、`RetracksPlayer.getExitReasons()`＝
  ネイティブ側のプロセス終了履歴と対になる形）から見られるようにした
- フォールバックUIは`i18next.t()`を直接使用（`useTranslation()`不可。
  Providerに依存しない一番外側に置くため。`src/i18n/index.ts`の初期化は
  モジュール読み込み時に同期的に終わるので、Providerの外でも使える）

**4. 境界条件の総当たり**（2026-09-11・着手・進行中）

- 権限が拒否された状態での境界条件を実機で総当たり中に、実際の不具合を発見
- **不具合：権限拒否時、「ライブラリを読み込んでいます」の無限スピナーから
  復旧不能**（2026-09-11・発覚・修正済み）
  - 再現手順：起動中に`adb shell pm revoke`でREAD_MEDIA_AUDIOを剥奪
    （OSが即座にプロセスをkill、これは正常なOS挙動）→再起動→権限ダイアログで
    「許可しない」（＝Androidの「今後表示しない」を伴う恒久拒否、
    `canAskAgain: false`）を選択
  - 症状：`library.loading`（読み込み中）表示のまま永久に止まる。デバッグ画面で
    確認すると「ライブラリ0曲／キュー0曲」、ログに「メディアの権限が
    許可されていません」、プロセス終了履歴に「権限の変更 permissions revoked」
    が正しく記録されている一方、アプリ内には状況を示すUIも復旧手段も皆無
  - 原因：`playback.tsx`の起動処理が`if (!(await requestPermission())) { ...; return; }`
    という早期returnで、`tracks`が空配列のまま止まり、権限を後から設定アプリで
    許可してもアプリに戻るだけでは検知されない構造だった
  - 修正：`src/library.ts`の`requestPermission()`/新設`checkPermission()`の
    戻り値を`boolean`から`{ granted, canAskAgain }`（`PermissionState`）に変更。
    `playback.tsx`にライブラリ読み込み処理を`loadLibraryAndSession()`として
    切り出し、起動時・`retryPermission()`（ユーザー操作）・`AppState`が
    `active`に戻った際の再確認（`checkPermission()`、ダイアログを出さない）の
    3箇所から呼べるようにした。`app/index.tsx`に`PermissionGate`を新設し、
    `canAskAgain`に応じて「許可する」（再ダイアログ）か「設定を開く」
    （`Linking.openSettings()`）のどちらかを出し分ける
  - 実機確認：権限を恒久拒否した状態で`PermissionGate`が表示されること、
    「設定を開く」からAndroidの設定画面（アプリ情報→権限→音楽とオーディオ）
    に遷移できること、そこで許可に変更して手動でアプリへ戻るだけで
    （明示的な再起動なしに）`AppState`経由でライブラリが自動的に再読み込み
    され、シャッフル再開位置も含めて正常に復元されることを確認

**3. 長時間ソーク**（2026-09-11〜12・実施・要点は判明、原因切り分けは次の実験待ち）

- **条件**：2026-09-12 01:16:55〜07:50:56（6時間33分59秒）。ホームボタンで
  バックグラウンドに送ってから画面ロック（`mWakefulness=Dozing`）。バッテリー
  100%・充電中。メドレー（RUSH）ON、区間は`src/rush.ts`の既定値
  （70秒）のまま変更せず。ホーム画面にウィジェットを3個設置（後述）
- **結果（良好）**：プロセスの終了履歴（`RetracksPlayer.getExitReasons()`）に
  テスト期間中の新規記録なし＝OSに一度もkillされず完走。アプリ内エラー
  履歴（`crashLog`）も記録なし。テスト終了時点でindex 900/992（`992`は
  当初`1051`曲だったキャッシュがバックグラウンド再走査で更新された後の数）
  として再生継続中。`adb logcat`にOOM/ANR/lowmemorykillerの記録なし
- **気になる点：Native Heapが増加**（Java Heapは横ばい）
  - 開始時（01:16:55）Native Heap Pss 24.4MB、Java Heap 36.5MB
  - 終了時（07:50:56）Native Heap Pss 113.3MB、Java Heap 37.2MB
  - TOTAL PSSは523MB→619MB（約634MB表記はRSS等含む生値、要約時はPss基準）
  - 曲の切り替わり回数で割ると：media session の active item id が
    567→900で333回切り替わり（71秒/回≒`DEFAULT_SEGMENT.lengthSec`70秒と
    ほぼ一致、メドレーが動いていた証跡）。86.8MB ÷ 333回 ≈
    **0.26MB/回**。「時間で割る」（13MB/時間）は曲送り頻度がRUSHの区間長
    次第で桁違いに変わるため単位として不適切。切り替わり回数で見るのが正しい
  - **原因の見立て**：Java Heapが動かずNative Heapだけ増えるのは、Android
    のBitmapがピクセルデータをネイティブメモリに持つ挙動と一致。曲が変わる
    たびにビットマップを作る箇所は2つ：
    (1) `RetracksWidgetProvider.kt`の`loadArtwork()`（`BitmapFactory.decodeStream`、
    RGB_565）と、正方形でないジャケットの場合のみ追加で1枚作る`padToSquare()`
    （`Bitmap.createBitmap`）。どちらも`recycle()`されずGC任せ
    (2) media3側の通知・ロック画面アートワーク（`setArtworkUri()`）
  - **追加で判明した点**：`RetracksWidgetProvider.updateAll()`は自プロバイダの
    ウィジェットIDだけに絞り込んで（`getAppWidgetIds(ComponentName(...))`）
    `buildViews(context)`をループ内で呼んでおり、`loadArtwork()`がウィジェット
    の数だけ重複デコードされる。`adb shell dumpsys appwidget`で確認した
    ところ、このテスト中ホーム画面にRE:TR4CKS自身のウィジェットが
    **3個**（id=17/18/19、いずれも`provider`欄が
    `com.yskms.retracks/...RetracksWidgetProvider`。他に無関係な
    Pulsar・Duolingo・Google検索のウィジェットも別途あったが、それらは
    このループに入らない）設置されていた。1個あたりの実測サイズは
    `views_bitmap_memory`で242,208バイト（≈236.5KB）。理論値
    （`ARTWORK_TARGET_PX=192`・RGB_565・最大384×384px≈288KB、正方形
    ジャケットなら`padToSquare()`は素通りするので追加の1枚は無し）と近い。
    3個ぶんの理論上限は236.5KB×3≈710KB/回で、実測0.26MB（266KB）/回は
    その約37%＝単体1個ぶんの実測サイズにかなり近い。つまり3回デコード
    されるうち大半はGCで回収されつつ、正味だいたい1個ぶんに相当する量が
    残り続けている、という中間的な読みになる
  - **未判定**：リーク（際限なく増え続ける）か、キャッシュ的に頭打ちになる
    ものかは、今回1回の計測だけでは判断できない。ウィジェット由来か
    media3の通知アートワーク由来かも未切り分け
- **次の実験（未実施）**：ウィジェットの有無以外は同条件（メドレーON・
  区間5秒・画面OFF）で30〜60分ずつ計測し比較する
  - 実験A：ウィジェットをホーム画面から外した状態
  - 実験B：ウィジェットを設置した状態（今回同様3個、または1個）
  - Aでも増えるなら通知アートワーク側、Aで平坦ならウィジェット側と切り分けられる
- **リリース判断への影響**：8章に記録した`MemoryLimiter:AnonSwap`の警告閾値
  （3GB）を基準にすると、今回の0.26MB/回が際限なく線形に続くと仮定しても
  3072MB ÷ 0.26MB ≈ 11800回の切り替わりが必要。これは区間長に強く依存する：
  メドレー既定の70秒/回なら約229時間（約9.6日）連続再生、最短区間
  （5〜7秒/回）なら切り替わり回数が10倍以上になるため約16〜22時間
  （約1日）で到達する計算になる。通常の使い方（既定70秒・断続的な利用）
  ではまず届かないが、「メドレー＋最短区間を丸1日つけっぱなし」という
  使い方だけは射程に入るため、9.6日だけを結論にせず区間長依存であることを
  併記する。もっとも際限なく線形に増え続けるかどうか自体が未確認なので、
  これらの日数は「今の観測ペースが変わらなければ」という上限の目安

### Phase 1（MVP）

- 再生
- 区間再生（RUSH）
- シャッフル ＋ **1巡状態の永続化**（→ 6.2）
- リピート
- フェードアウト
- バックグラウンド対応
- ライブラリ画面（アーティスト / アルバム / 楽曲タブ）
- 長押し複数選択によるキュー作成（→ 10.3）

### Phase 2

**棚卸し（2026-09-11）**：以下は実装済みだったため一覧から外した
（検索 → 10章 `app/search.tsx`、タブの並び替え・表示非表示設定 → 10.6、
1巡の進捗表示 → 6.3 `player.queuePosition`）。この一覧は「次に何を
やるか」を判断するときに読むものなので、済んだ項目を残したままにすると
判断を誤らせる。

**スコープ外化（2026-09-11）**：以下は要らないと判断したため一覧から外した。
区間指定の％対応・曲ごとの個別設定（→ 4.2 / 4.3、15章1・5・11・13番）、
お気に入り（→ 10.2 / 10.4。端末を移すと持ち運べないため）。

- プレイリストタブ（自動プレイリスト / ユーザー作成。→ 10.4、15章16番）
- 選択内容のプレイリスト保存
- UI改善
- 設定強化（10.6の高優先度4項目とRUSHのデフォルト値は対応済み。アクセント
  カラーの変更が残り。ギャップレス再生は将来検討へ降格）
- リリース準備（安定性向上。2026-09-11、このアプリで実際に起きた壊れ方
  ——OSによる強制終了・1曲キューの無限ループ・復元データの巻き戻り——から
  具体化。詳細・進捗は13章）
  1. リリースビルド＋自前キーストア（最優先。署名は後から変更できない）
  2. JS側のエラーバウンダリ（1つも無く、レンダー中の例外でアプリ全体が
     落ちる。リリースビルドでは何も表示されず、原因もどこにも残らない）
  3. 長時間ソーク（画面OFF/バックグラウンドで3〜6時間再生し続け、プロセス
     終了履歴を確認。プレイヤー画面前面表示時の残り12〜13MB/分の実測も）
  4. 境界条件の総当たり（曲0件・曲1件・権限拒否→後から許可・再生中の権限
     剥奪・走査中の画面遷移・SDカード抜去・イヤホン抜き差し・ウィジェット
     起動時のキュー復元、など）
  - 順番は 1 → 2 →（3を裏で流しながら）4。1・2が無いと3・4の結果を
    拾えないため
  - 音声フォーカス（着信・他アプリの音での一時停止/復帰）とバックグラウンド
    時の節約（ポーリング停止・画像キャッシュ解放）は対応済み（→ 13章）

### Phase 3（将来検討）

**棚卸し（2026-09-11）**：曲ごとの個別設定はスコープ外に決定（→ 4.2 / 4.3、
15章）。カバー画像表示は既に実装済み（→ 10.2「大きなアートワーク表示」）。
カウントダウン表示・ジェスチャー操作は具体的な仕様が残っておらず、必要に
なったときに改めて検討することとして一覧から外した。

- ダークモード対応（→ 10.6 将来検討）

---

## 15. 要確認事項（未決定・曖昧な点）

たたき台を読んでいて、実装に落とす前に決めておいた方がよさそうな点です。

1. ~~**区間指定は固定秒数か、曲の長さに対する相対（%）か**~~ → **決定（2026-09-02）／2026-09-11に％対応を撤回**
   秒・％の両方に対応する方針だったが、2026-09-11に％対応は不要と判断し撤回。秒のみ対応。→ 4.2
2. ~~**開始位置＋再生時間が曲の長さを超える場合の挙動**~~ → **決定（2026-09-02）**
   曲の終端でフェードして次の曲へ。開始位置自体が曲より長い場合は0秒から再生。→ 4.2
3. ~~**自動スキップOFF時の挙動**~~ → **決定（2026-09-02）**
   自動スキップ＝RUSHモードのON/OFF。OFF時は区間再生を無効化し、曲を頭からフル再生する（A案）。→ 4.3
4. ~~**リピートの粒度**~~ → **決定（2026-09-02）／優先度：低**
   キュー全体のリピートのみ実装する。1曲リピートは「あってもなくてもよい」レベルのため対象外とし、必要になったら Phase 3 で検討する。
5. ~~**区間設定はキュー全体の共通設定か、曲ごとの個別設定か**~~ → **決定（2026-09-02）／2026-09-11に個別設定を却下**
   全体設定を基本とし、曲ごとの個別設定を上書きとして持てる方針だったが、2026-09-11に個別設定は実装しないと決定。全体設定のみ。→ 4.2 / 4.3
6. ~~**曲一覧画面からの手動再生時、区間再生設定は適用されるか**~~ → **決定（2026-09-02）**
   普通のプレイヤー準拠とする。一覧から曲をタップしたら**その一覧をキューとして再生開始**し、RUSH ON なら区間再生、OFF ならフル再生。割り込み再生という特別扱いは設けない（シャッフル順は 6.2 の永続化で保持されるため不要）。→ 10.4
7. ~~**キュー作成後の画面遷移**~~ → **決定（2026-09-02）**
   「作成」押下後はプレイヤー画面へ自動遷移し、再生を開始する。→ 10.3
8. ~~**Android 13+ のメディア権限（READ_MEDIA_AUDIO）まわりの許可フロー**~~ → **決定（2026-09-02）**
   初回起動時にリクエストし、拒否時は説明画面＋設定アプリへの導線を表示する。→ 3.1
9. ~~**再生中に RUSH を ON/OFF 切り替えたときの挙動**~~ → **決定（2026-09-02）**
   次の曲から適用する。再生中の曲のシークし直しは行わない。→ 4.3
10. ~~**プレイヤー画面のスライダー操作は「全体設定の変更」か「その曲の個別設定の作成」か**~~ → **決定（2026-09-02）**
    スライダーは常に全体設定の変更。→ 4.2
11. ~~**個別設定の識別キーと孤児データの掃除**~~ → **却下（2026-09-11）**
    個別設定自体を実装しないと決定したため、この論点は不要になった。
12. ~~**キュー作成画面とライブラリ画面の役割が重複しないか**~~ → **決定（2026-09-02）**
    専用画面は廃止し、ライブラリ画面の**長押し→複数選択**に統合する。アーティスト／アルバムをまとめて選んで簡易プレイリストとして流す導線は必須機能。→ 10.3
13. ~~**秒⇄％ を切り替えたときの既存値の扱い**~~ → **却下（2026-09-11）**
    ％対応自体を実装しないと決定したため不要になった。
14. ~~**再生時間（Length）よりフェード時間（Fade）が長い場合のクランプ**~~ → **決定（2026-09-02）**
    実効フェード時間 = `min(設定値, Length ÷ 2)`。→ 4.4
15. ~~**RUSH を OFF にした瞬間、再生中の曲の区間カットも解除するか**~~ → **決定（2026-09-02）**
    解除する。OFF時のみ現在曲のカットも即座に解除し、そのまま最後まで再生する（シークはしない）。→ 4.3
16. **自動プレイリスト「最近再生された」は作るか**（2026-09-11、保留）
    再生履歴（曲ごとの最終再生日時）を持たせる価値があるか判断がついていない。作る場合は「最も再生された」と同様、再生のたびに何か書き込む設計が要る。→ 10.4、14章 Phase 2「プレイリストタブ」

---

## 変更履歴

| バージョン | 日付 | 内容 |
|---|---|---|
| v0.1 | 2026-09-02 | 第1案（UI設計書）・第2案（要件定義書）を統合して初版作成 |
| v0.2 | 2026-09-02 | 区間指定方式（秒ベース）と設定スコープ（全体共通）を決定し 4.2 / 15章に反映 |
| v0.3 | 2026-09-02 | 自動スキップをRUSHモードとして再定義（A案）、％対応と個別設定の適用条件を確定、新規論点 9〜13 を追加 |
| v0.4 | 2026-09-02 | スライダー＝全体設定、RUSH切替は次曲から、Fadeクランプを確定。曲ごとの個別設定を Phase 3 の保留扱いに変更 |
| v0.5 | 2026-09-02 | 区間の境界処理・リピート粒度・キュー作成後の遷移・権限フロー・RUSH OFF時の即時解除を確定。残る未決定は #6（曲一覧の役割）と #12（秒⇄％の値変換）のみ |
| v0.6 | 2026-09-02 | 6章を全面改訂し「1巡状態の永続化」をMVPに追加。普通のプレイヤー部分を Pulsar ベースで具体化（タブ構成・設定画面・再生中画面）。#6 を決定 |
| v0.7 | 2026-09-02 | キュー作成専用画面を廃止し、長押し複数選択に統合（#12決定）。アルバムタブを追加。2巡目は順列を作り直す方針を確定 |
| v0.8 | 2026-09-02 | 13章を技術選定の結果で全面改訂（expo-audio + expo-music-library）。通知の次の曲が未解決課題として残る |
| v0.9 | 2026-09-02 | expo-audio がイヤホン/通知の「次の曲」を構造的に塞いでいることをネイティブソースで確認。再生層を自前の Kotlin + Media3 モジュールで持つ方針に変更 |
| v1.0 | 2026-09-03 | 実機検証で通知/イヤホンの「次の曲」が動作することを確認し、技術構成を確定。13.4 を検証結果に更新 |
| v1.1 | 2026-09-03 | フェードインを追加、クロスフェードは対象外と決定。区間の切り出しを ClippingConfiguration へ変更 |
| v1.2 | 2026-09-03 | フェードの不具合を解消（音量の適用先が再生位置より先行する問題）。技術検証6項目すべて合格 |
| v1.3 | 2026-09-06 | 再生が突然止まる症状の調査結果を8章に記録。原因はウィジェットからサービスを起こす際のクラッシュ。メモリは同種アプリと同等と確認 |
| v1.4 | 2026-09-09 | 多言語化（ja/en）の基盤を追加。ウィジェットから起こしたときに通知・ロック画面が出ない不具合を8章に記録（原因は POST_NOTIFICATIONS 未リクエストと MediaSession の addSession 未呼び出し、修正済み） |
| v1.5 | 2026-09-10 | 設定画面を新設し、10.6の高優先度項目のうち「短い曲を除外」（既定ON・5秒に変更）「並べ替え順（The/A/An無視）」と、要件外だった「言語の手動切り替え」を実装。海外展開を前提とする方針を明記し、10.6にライト/ダーク・メインカラー・課金案内の調査結果を追記。起動時のスプラッシュをAndroid標準のアイコンアニメーション（端末によって二重に見える）からJS制御のフェードに差し替え |
| v1.6 | 2026-09-10 | 10.6の「タブ項目の変更（表示/並び替え）」を実装。全曲シャッフルFABを曲タブの中からライブラリ画面レベルへ移し（副次的にアーティスト/アルバムタブでも使えるようになった）、検索結果のセクション見出しをタブ設定から独立させた |
| v1.7 | 2026-09-10 | `expo-music-library` が MediaStore の IS_MUSIC を一切見ていない発見を記録し、自前ネイティブモジュールに `getTrackFolders()` を追加。10.6に「音楽以外のファイルを除外」（既定ON）を実装。除外フォルダの設計を `getFoldersAsync()` 非依存の方針に変更し、アーティスト/アルバムタブの独立クエリ問題を将来検討として切り出した |
| v1.8 | 2026-09-11 | 「短い曲を除外」の既定値をOFFへ戻した（「音楽以外のファイルを除外」により既定ONにしていた主因が解消されたため）。`getTrackFolders()` 追加後の走査時間を実測（1027曲/916ms、劣化なし）し13.4に追記。復元したネイティブキューの Track に isMusic/folderId/folderName の既定値を補うよう修正 |
| v1.9 | 2026-09-11 | 10.6の「除外するフォルダ」を実装。専用画面（Pulsar参考のUI、候補はMediaStore認識済みフォルダのみ。`MANAGE_EXTERNAL_STORAGE` はGoogle Play審査上避けた）を追加 |
| v2.0 | 2026-09-11 | 14章のPhase 2一覧を棚卸し。実装済み（検索・タブの並び替え・1巡の進捗表示）を外し、「次に何をやるか」の判断材料として現状に合わせた |
| v2.1 | 2026-09-11 | アーティスト/アルバムタブと詳細画面を、独立した MediaStore 問い合わせ（`getArtists()`等6関数）から絞り込み済み `tracks` の導出へ作り替え、「短い曲を除外」等の設定がこの2タブにも及ぶようにした。アルバム詳細の曲順をMediaStoreの不定順からディスク/トラック番号順に修正。10.6の但し書きと将来検討からこの課題を除去 |
| v2.2 | 2026-09-11 | Phase 2/3のスコープを整理。区間指定の％対応・曲ごとの個別設定・お気に入りを不要と判断し削除（4.2 / 4.3 / 10.2 / 10.4 / 15章）。ロック画面アートワークは既に実装済みの自動的な挙動（設定項目ではない）と判明したため10.6から削除。Phase 3の「カバー画像表示」は実装済み、「カウントダウン表示」「ジェスチャー操作」は仕様が無いため削除。テーマカラーを「ライト/ダーク切り替え」（重い・将来検討のまま）と「アクセントカラーの変更」（アプリ再起動での反映を許容する前提で優先度：中〜低へ）に分割し、後者の実装規模（対象11ファイル42箇所、うち`StyleSheet.create()`内が約23箇所、ネイティブ側1箇所）を調査して記録 |
| v2.3 | 2026-09-11 | RUSHのデフォルト値（`src/rush.ts` の `DEFAULT_SEGMENT`）を再生時間35秒→70秒に変更。「初期値に戻すボタン」は不要と決定し、定数を直接編集する運用に確定（10.6・14章）。ギャップレス再生は不満が無いため優先度：中〜低から将来検討へ降格 |
| v2.4 | 2026-09-11 | 再生中にネイティブヒープが増え続けOSに強制終了される不具合を修正（`fix/status-rerender-memory-leak`）。原因は`usePlayback()`のContext valueを毎レンダー作り直していたことで、250msごとのstatus更新で全画面が再レンダーされ続けていた。status用の別Contextへの分離・value のuseMemo化・プレイヤー画面のシークバーとキュー行のReact.memo化・不要なstatus更新のスキップで対処。13章に症状・原因・切り分け方・実測値・残件を記録 |
| v2.5 | 2026-09-11 | Unicode文字で描画していたアイコン類（⌕⚙⊞▦☰←✕＋▾⤮✓）を`@expo/vector-icons`（Ionicons）へ全画面で置き換え（`feat/header-icons`）。ホーム画面ウィジェットの操作アイコンも24dp→26dpへ拡大。10.7にアイコン方針を追記 |
| v2.6 | 2026-09-11 | プレイヤー画面の区間設定（Start/Length/FadeIn/FadeOut）を折りたたみ可能にし、既定を閉じた状態に変更（`feat/collapsible-segment-settings`）。「この曲での実効区間」プレビュー表示を削除し、関連する未使用コード・i18nキーを整理。10.2の要素表を更新 |
| v2.7 | 2026-09-11 | プレイヤー画面「区間設定」を「再生区間設定」に改称。ライブラリ画面FABの「続きから」表示を「シャッフル再開」に改称（単位語は省略）。RUSHトグルのUI表示名を「RUSH」から「メドレー」に変更（初見で機能が伝わらないため）。内部名（`rushOn`・`src/rush.ts`・本書の呼称）はRUSHのまま据え置き、4.3に橋渡しの注記を追加 |
| v2.8 | 2026-09-11 | プレイヤー画面ヘッダーの「1巡」表示（「1」が巡数として固定表示され3巡目以降も不正確だった）を、キュー内の位置（`status.index + 1` / `queue.length`）に置き換え。シャッフルキュー・順番再生キューのどちらでも同じ式で出せるため、「1巡」も「シャッフル」も語として不要になった。`player.roundProgress`キーは削除。デバッグ画面（状態カード）は永続化されたシャッフル状態を見せる場所として「1巡」表示のまま据え置き。ライブラリ画面FABは「全曲シャッフル再開」に改称し、スコープ（全曲か今のキューか）を語で区別できるようにした。内部の呼称（6.2・`reconcile()`等）は「1巡」のまま据え置き、6.2冒頭に橋渡しの注記を追加 |
| v2.9 | 2026-09-11 | 曲送り操作からUI反映まで最大1秒かかる不具合を修正（`fix/track-change-status-latency`）。真因はネイティブ側`getStatus()`が最大200ms古いスナップショットを返すことで、JS側だけの対処（`setStatus(getStatus())`の即時呼び出し）では体感が変わらなかった。`refreshSnapshot()`を切り出し、曲の切り替わり・リピート設定変更の直後に同期更新するよう修正（ネイティブ再ビルド）。同根の別バグ（リピート連打で巡回が進まない）も同時に解消。真因が分かる前に投機的に追加したジャケット先読みは効果未検証のままリバート。13章に切り分けの経過を記録し、`RetracksPlayerModule.ts`の`getStatus()`宣言にも注記を追加 |
| v2.10 | 2026-09-11 | アルバム詳細画面の曲一覧にトラック番号を表示（`feat/album-track-numbers`）。`Row`に`trackNumber`プロパティを追加し、未タグの曲は空欄のまま（一覧位置からの補完はしない） |
| v2.11 | 2026-09-11 | 曲・アルバムタブに並べ替え（軸＋昇順/降順）を追加（`src/sortOrder.ts`、10.4）。プロバイダの`tracks`/`albums`は正本のまま変更せず、各タブが表示用に並べ替えた配列を別に持つ形にした（SettingsProviderに入れると全画面の再導出が連鎖するため意図的に分離）。第2・第3キーに`compareByTrackOrder()`を流用し、アルバム/アーティスト順でもアルバム内の曲順が保たれるようにした。アーティストタブ・詳細画面の一覧・「追加日」軸はフェーズ2として明記。設定画面の見出しを「並べ替え」→「並べ替えの規則」に変更（軸の選択と規則の違いを区別するため） |
| v2.12 | 2026-09-11 | アーティスト詳細の曲一覧に並べ替え（タイトル/アルバム/長さ/発売年）を追加し、既定をタイトル順からアルバム順に変更（ディスコグラフィとして読みやすいため）。並べ替えメニューを`src/components/SortMenu.tsx`として切り出し、曲・アルバムタブと共通化。`sortTracks()`に`year`軸（曲が属するアルバムの発売年で比較）を追加。ライブラリ画面のアーティストタブ・アルバム詳細の曲一覧・アーティスト詳細のアルバム一覧（発売年降順で固定）・「追加日」軸は引き続き対象外（10.4） |
| v2.13 | 2026-09-11 | リリース準備（安定性向上）に着手。14章Phase 2の「安定性向上」を4項目に具体化し、1・2に対応（13.5）。(1) 自前の署名キーストア（`android/app/release.jks`＋`android/keystore.properties`、いずれもリポジトリ管理外）を`keytool`で作成し`build.gradle`に配線。`./gradlew assembleRelease`の成功・`apksigner`での署名確認・実機インストールでの起動/権限/走査/再生確認まで完了。(2) JSのエラーバウンダリ（`src/components/ErrorBoundary.tsx`）を`app/_layout.tsx`最外部に追加。捕まえた例外は`src/crashLog.ts`経由でAsyncStorageに残し、デバッグ画面に新しい「アプリ内エラーの履歴」カードとして表示。手動でエラーを発生させ、フォールバック表示とログ記録の両方を実機確認。残る長時間ソーク・境界条件の総当たりは未着手 |
| v2.14 | 2026-09-11 | 境界条件の総当たり（項目4）に着手し、実機テストで不具合を発見・修正（`fix/permission-denied-recovery`）。権限を恒久拒否（`canAskAgain: false`）した状態で起動すると「ライブラリを読み込んでいます」の無限スピナーから復旧不能だった問題を修正。`src/library.ts`の`requestPermission()`/新設`checkPermission()`が`{ granted, canAskAgain }`を返すよう変更し、`playback.tsx`にライブラリ読み込みを`loadLibraryAndSession()`として切り出して起動時・手動リトライ・`AppState`復帰時の3箇所から呼べるようにした。`app/index.tsx`に`PermissionGate`を新設し「許可する」/「設定を開く」を出し分け。設定アプリで許可してアプリに戻るだけで（再起動なしに）自動復旧することを実機確認。詳細は13.5 |
| v2.15 | 2026-09-12 | 長時間ソーク（項目3）を実施。6時間34分（画面ロック・バックグラウンド、メドレー既定70秒区間）でOS強制終了ゼロ・クラッシュゼロを確認した一方、Native Heapが24MB→113MB（333回の曲切り替わりで0.26MB/回）と増加する傾向を発見。原因は`RetracksWidgetProvider.kt`の`loadArtwork()`/`padToSquare()`が`recycle()`されずGC任せなこと、および`updateAll()`がウィジェットIDごとに`buildViews()`をループ呼び出しするため（実機のホーム画面にウィジェットが3個設置されており重複デコードが発生）と推定。リークか頭打ちかは未判定で、ウィジェット有無を比較する追加実験を13.5に計画として記録 |
